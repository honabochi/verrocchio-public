# Remote return point

No Owner action is required while away from the computer.

## Verified now

- The public repository is reachable without authentication.
- The repository includes the source, required assets, setup instructions, and
  a visible MIT license.
- A fresh isolated clone of local release packet `6be1a47` completed a clean
  dependency install, all 117 automated tests, and a production build on
  2026-09-01.
- Registration is complete. Personal registration answers remain
  off-repository.
- The English demo script, timed subtitle file, and YouTube upload packet are
  ready.
- The Owner reports that the `NoBu Builds` channel, prepared banner, and
  bilingual About text are configured. No channel setup needs to be repeated.
- The VERROCCHIO Site access mode is public. Version 25 returns HTTP 200 without
  authentication and identifies deployed product source `cbca8ad`.
- The Version 25 client bundle contains the current host-request handoff and
  browser-storage disclosure. An earlier deployed Chrome smoke passed the
  four-tool surface, FERMO/resume, CLAIMED/VERIFY boundary, mobile overflow,
  404, and runtime-error checks; it is not final-revision evidence. See
  `evals/hosted-smoke-2026-08-31.json`.

## Intentionally incomplete

- Local release-packet commits after `cbca8ad` are not yet synchronized to the
  public GitHub default branch or Site revision marker.
- Natural-language host selection across the fixed seven cases and the two DOM
  comparisons have not been rerun against a synchronized frozen revision.
- No public YouTube video is recorded.
- No final annotated submission tag exists.
- Owner-only Devpost answers and final submission remain pending.

## Exact next action

1. Synchronize one approved release packet to public GitHub and the Site, then
   verify that both public revisions identify the approved source.
2. Run the fixed seven natural-language host cases on that public Site.
3. Record the two required DOM comparisons.
4. Export and machine-check the resulting evaluation receipt.

Codex then hands back the recording-ready build.
After the public video URL is known, the release packet can be frozen, tagged,
read back, and submitted by the Owner.

For the account-safe upload sequence, title, description, and settings, use
[`YOUTUBE_UPLOAD_PACKET.md`](YOUTUBE_UPLOAD_PACKET.md). The existing Google
Account may be reused with a separate public YouTube channel identity; account
creation, channel selection, upload, and publication remain Owner-only.
