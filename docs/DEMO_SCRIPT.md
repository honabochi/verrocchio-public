# Demo script · target 2:45

Status: Phase 1 shots are executable; G3 shots remain provisional until the
payload FIRMA is given.

## Recording rules

- English voiceover.
- One continuous judge path; no architecture slides.
- Keep the browser zoom and sample state fixed.
- Show one real GPT-5.6 response ID on screen.
- End on observable evidence, not a promise.

## Shot list and voiceover

### 0:00–0:18 · The missing finisher

**Screen:** VERROCCHIO opens on GIORNATE with MANCA and the launch window.

**Voiceover:**

> AI made me faster at producing code, but no AI owned the finish line. For my
> first hackathon I had too many tools, too many ideas, and one irreversible
> deadline. So I built VERROCCHIO: a workshop that measures only what is still
> missing from submission.

### 0:18–0:43 · Proof instead of progress

**Screen:** Attempt to close `Working product` without evidence. ATTACH PROOF
opens and MANCA does not change.

**Voiceover:**

> A task cannot become complete because someone clicked a checkbox. Every
> MANCA gate needs proof. Without a runnable path, response ID, repository,
> video, or submission receipt, the workshop refuses to count it.

### 0:43–1:12 · GPT-5.6 governs the boundary

**Screen:** Open the verified CAPOBOTTEGA decision for publishing. Show
`AFFRESCO`, `FIRMA REQUIRED`, the reason, next stroke, and response ID. Close
the dialog and show `LOCKED BY FIRMA`.

**Voiceover:**

> GPT-5.6 Sol is the CAPOBOTTEGA. It receives the contract, the proposed
> stroke, and the missing proofs, then returns strict structured output. It
> classified public deployment as AFFRESCO—an irreversible action—so the
> system stopped. Neither resume nor another AI decision can overwrite the
> human signature boundary.

### 1:12–1:33 · The system builds itself

**Screen:** Open EVIDENCE. Point to the real model response ID and the Codex
task ID.

**Voiceover:**

> This is not a diagram of a future workflow. VERROCCHIO used itself to govern
> its own implementation. Codex built and tested the workshop; GPT-5.6 stopped
> its publication; both actions became exportable evidence.

### 1:33–1:52 · From system to workpiece

**Screen:** Open CARTONE. Switch LA PRIMA MANO → VASARI → IL COLORISTA. Show
the compiled packet.

**Voiceover:**

> The same decision becomes a bounded work packet. Changing the actor changes
> the duty and stop rule, but never the commission. This is how the outer
> workshop controls a distinct inner product.

### 1:52–2:27 · G3 payload reveal

**Screen:** Open the Reading Constellation judge sample. Select the two-person
view, focus two different books connected by a theme, then reveal the
GPT-5.6-generated bridge recommendation and its source books.

**Voiceover:**

> The workpiece is Reading Constellation. Two people can read completely
> different books and still discover the theme between them. GPT-5.6 does not
> flatten their tastes into one score. It explains a bridge to the next book
> and points back to the books and themes that support the recommendation.

### 2:27–2:45 · Close the loop

**Screen:** Return to VERROCCHIO EVIDENCE, then CENACOLO. Show the payload
proof and remaining FERMO rows.

**Voiceover:**

> The product proves the workshop can direct something other than itself, and
> the workshop proves how the product was made. MANCA reaches zero only when
> judges can verify the complete pair. VERROCCHIO is the workshop that paints
> itself—and then ships the next work.

## Capture checklist

- [ ] Real hosted VERROCCHIO path
- [ ] Proofless gate refusal
- [ ] Real GPT-5.6 response ID
- [ ] Locked FIRMA state
- [ ] CARTONE role switch and packet
- [ ] Hosted G3 sample journey
- [ ] G3 GPT-5.6 response and traceable reasons
- [ ] Final EVIDENCE export and CENACOLO
- [ ] English audio, captions, public YouTube, ≤ 3:00
